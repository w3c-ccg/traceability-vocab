const document = require('../data/vc/credentialstatus.json');

const getRevocationList2020Status = () => {
  const example = document;
  return example;
};

module.exports = { getRevocationList2020Status };
